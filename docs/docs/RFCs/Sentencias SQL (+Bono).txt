RFC1)
SELECT OPERADOR.NOMBRE, OFERTA.VALOR AS INGRESOS
FROM 
OPERADOR INNER JOIN (RESERVA INNER JOIN OFERTA ON RESERVA.IDOFERTA = 
OFERTA.IDOFERTA) ON OPERADOR.IDOPERADOR = OFERTA.IDOPERADOR

WHERE RESERVA.ESTADORESERVA='finalizado'

RFC2)
SELECT TOP_POPULA, IDOFERTA, TIPODEOFERTA, DISPONIBLE, VALOR, FECHA_INICIO, FECHA_FINAL
FROM(

    SELECT OFERTA.IDOFERTA AS IDOFERTA, OFERTA.TIPODEOFERTA AS TIPODEOFERTA, OFERTA.CANTIDADDISPONIBLE AS DISPONIBLE,
    OFERTA.VALOR AS VALOR,OFERTA.FECHAINICIO AS FECHA_INICIO,OFERTA.FECHAFINAL AS FECHA_FINAL,
    
    COUNT(OFERTA.IDOFERTA) AS RESERVAS ,ROW_NUMBER() OVER (ORDER BY  COUNT (OFERTA.IDOFERTA) DESC)AS TOP_POPULA
    
    FROM OFERTA INNER JOIN RESERVA ON RESERVA.IDOFERTA = OFERTA.IDOFERTA 
    GROUP BY OFERTA.IDOFERTA, OFERTA.TIPODEOFERTA, OFERTA.CANTIDADDISPONIBLE, OFERTA.VALOR, OFERTA.FECHAINICIO,OFERTA.FECHAFINAL
    ORDER BY OFERTA.IDOFERTA)

WHERE TOP_POPULA<20;


RFC3) 
SELECT OFERTA.IDOFERTA, 100-(100/OFERTA.CANTIDADINICIAL)*OFERTA.CANTIDADDISPONIBLE AS PORCENTAJE_OCUPACION
FROM OFERTA

RFC4)
SELECT OFERTA.IDOFERTA
FROM OFERTA
WHERE OFERTA.FECHAINICIO>='01/06/18' AND OFERTA.FECHAFINAL<='30/12/18'
----------------------------------------------------------------------------------------------------------------------------
SELECT * 
FROM(
SELECT OFERTA.IDOFERTA, OFERTA.FECHAINICIO, OFERTA.FECHAFINAL, SERVICIO.NOMBRESERVICIO AS SERVICIOS
FROM OFERTA INNER JOIN SERVICIO ON SERVICIO.IDOFERTA = OFERTA.IDOFERTA
WHERE OFERTA.FECHAINICIO>='01/03/18' AND OFERTA.FECHAFINAL<='30/12/18' 
)


RFC5)
SELECT PERSONAHABILITADA.IDPERSONAHABILITADA, PERSONAHABILITADA.NOMBRE, OFERTA.TIPODEOFERTA AS CARACTERISTICA, OFERTA.DURACION AS DIAS_CONTRATADOS, OFERTA.VALOR AS VALOR_DIA
FROM ((PERSONAHABILITADA INNER JOIN RESERVA ON RESERVA.IDPERSONAHABILITADA = PERSONAHABILITADA.IDPERSONAHABILITADA)
INNER JOIN OFERTA ON RESERVA.IDOFERTA = OFERTA.IDOFERTA)
WHERE RESERVA.ESTADORESERVA='finalizado';


RFC6)
SELECT PERSONAHABILITADA.NOMBRE, OFERTA.TIPODEOFERTA AS CARACTERISTICA, OFERTA.DURACION AS DIAS_CONTRATADOS, OFERTA.VALOR AS VALOR_DIA
FROM ((PERSONAHABILITADA INNER JOIN RESERVA ON RESERVA.IDPERSONAHABILITADA = PERSONAHABILITADA.IDPERSONAHABILITADA)
INNER JOIN OFERTA ON RESERVA.IDOFERTA = OFERTA.IDOFERTA)
WHERE RESERVA.ESTADORESERVA='finalizado' AND PERSONAHABILITADA.NOMBRE LIKE 'German Enrique%';